- import type { Card, BorderRarityKey } from '@types/index';
+ import type { Card, BorderRarityKey } from '@/types';
- import { BORDER_RARITY_LABELS } from '@types/index';
+ import { BORDER_RARITY_LABELS } from '@/types';
